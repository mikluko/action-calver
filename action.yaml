---
name:        "CalVer"
description: "Opinionated CalVer versioning"
branding:
  color: "white"
  icon:  "calendar"
inputs:
  token:
    description: "GitHub token (use {{ github.token }})"
    required:    true
outputs:
  series:
    description: "series"
  version:
    description: "version"

runs:
  using: docker
  image: docker://ghcr.io/mikluko/action-calver:v24.12.8-pr4.48.ae495dc
  env:
    GH_TOKEN: ${{ inputs.token }}
