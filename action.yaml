---
name:        "CalVer"
description: "Opinionated CalVer versioning"
branding:
  color: "white"
  icon:  "calendar"
inputs:
  token:
    description: "GitHub token (use {{ github.token }})"
    required:    true
outputs:
  series:
    description: "series"
  version:
    description: "version"

runs:
  using: docker
  image: docker://ghcr.io/mikluko/action-calver:v24.12.9-pr5.50.829e3ab
  env:
    GH_TOKEN: ${{ inputs.token }}
